# define three functions

# The arithmetic mean
my_mean <- function(x) {}

# Covariance
my_cov <- function(x, y=x) {}

# Pearson's correlation
my_cor <- function(x, y) {}

# test that they give the same results as mean, cov, cor
# (at least for numeric vector arguments)
